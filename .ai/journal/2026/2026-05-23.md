# Journal — 2026-05-23

## Summary

Changed the Vite dev server port from 5173 to 3000, then hardcoded `base: '/todo-app/'` in `vite.config.ts` to match the GitHub Pages deployment path exactly.

## Changes Made

- `vite.config.ts` — set `server.port: 3000`; set `base: '/todo-app/'`
- `scripts/snapshot.js` — updated `BASE_URL` for new base path
- `docs/getting-started.md` — updated port and base path references
- `README.md` — updated localhost links

## Decisions

- Set `server.port: 3000` in `vite.config.ts` rather than passing `--port` in the npm script, keeping config centralised.
- Hardcoded the base path instead of using the `GITHUB_REPOSITORY` env-var fallback so local dev matches production exactly, avoiding a class of routing/asset bugs that only surface on GitHub Pages.

## Open Questions

- Run `npm run snapshot` to verify the snapshot script navigates correctly under the new base path.
