# 2026-05-23

## What I worked on

Changed the Vite dev server port from the default 5173 to 3000. Updated `vite.config.ts`, `scripts/snapshot.js`, `docs/getting-started.md`, and `README.md` to reflect the new port.

## Decisions made

Set `server.port: 3000` in `vite.config.ts` rather than passing `--port` in the npm script, keeping the config centralised.

## Next step

Commit the port change and updated docs/snapshot together.

---

## What I worked on

Set `base: '/todo-app/'` in `vite.config.ts` (replacing the dynamic `GITHUB_REPOSITORY` approach). Updated `scripts/snapshot.js` `BASE_URL`, and localhost links in `README.md` and `docs/getting-started.md`.

## Decisions made

Hardcoded the base instead of using the env-var fallback so local dev matches production exactly, avoiding a class of routing/asset bugs that only surface on GitHub Pages.

## Next step

Run `npm run snapshot` to verify the snapshot script navigates correctly under the new base path.
